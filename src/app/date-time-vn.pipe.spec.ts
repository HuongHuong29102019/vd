import { DateTimeVNPipe } from './date-time-vn.pipe';

describe('DateTimeVNPipe', () => {
  it('create an instance', () => {
    const pipe = new DateTimeVNPipe();
    expect(pipe).toBeTruthy();
  });
});
